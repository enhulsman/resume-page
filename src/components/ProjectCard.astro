---
const { title, description, href, tags = [] } = Astro.props;
---
<article class="border rounded-lg p-4 hover:shadow-lg transition-shadow">
  <h3 class="text-lg font-medium"><a href={href}>{title}</a></h3>
  <p class="text-sm text-slate-600 mt-2">{description}</p>
  <div class="mt-3 flex flex-wrap gap-2">
    {tags.map(t => (
      <span class="text-xs px-2 py-1 bg-slate-100 rounded">{t}</span>
    ))}
  </div>
</article>
