---
const { name = 'Ezra Hulsman', role = 'Allround Developer', avatar = '/avatar.svg', location = 'Earth' } = Astro.props;
---
<header class="py-6">
  <div class="flex items-center gap-5">
    <img src={avatar} alt={name} width={64} height={64} class="h-16 w-16 rounded-full ring-2 ring-slate-200 object-cover" />
    <div>
      <h1 class="text-2xl sm:text-3xl font-semibold tracking-tight text-slate-900">{name}</h1>
      <p class="text-sm text-slate-600">{role} · {location}</p>
      <p class="text-xs text-slate-500 mt-1">Python · React · Rust · C/C++ · Java · Bash</p>
    </div>
  </div>
</header>
